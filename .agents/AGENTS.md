# Custom Rules for AlphaX CRM

- **Automatic Live Deployment:** Whenever code changes or feature updates are completed in this workspace, automatically create a clean commit and run `git push origin main` so that Streamlit Cloud (`https://alphax-crm-hqowhgixskwx9mmsrzf32r.streamlit.app`) updates live immediately.
